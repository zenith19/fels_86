<h2 class="center"><%= t :users_title %></h2>
<%= will_paginate %>
<ul class="users">  
  <% @users.each do |user| %>
  <li>
    <h3>
      <%= link_to user.firstname + " " + user.lastname, user_path(user.id) %>
    </h3>
  </li>
  <% end %>
</ul>
<%= will_paginate %>